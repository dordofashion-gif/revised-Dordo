STELLAR FONT INSTALLATION

Copy your commercially licensed Stellar font file into this folder and name it exactly:

stellar.ttf

Expected final path:
public/fonts/stellar.ttf

The website CSS is already configured to apply Stellar only to the primary hero headline on each page.
The font file itself is intentionally not duplicated in this project package.
